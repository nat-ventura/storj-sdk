#!/usr/bin/expect -f

spawn storj login

expect {
  "email" {
    send "$env(STORJ_BRIDGE_USERNAME)\n"
  }
  "already paired" {
    send_user "Already logged in"
  }
  "password" {
    send "$env(STORJ_BRIDGE_PASSWORD)\n"
  }
  "successfully paired" {
    send_user "success"
  }
}

